import { EntityDetailPage } from "@/components/workflow/demo-pages";

export const metadata = {
  title: "Detail Entitas | HAWKEYE",
  description: "Panel detail entitas, risk signal, confidence, dan bukti terkait.",
};

export default function Page() {
  return <EntityDetailPage />;
}
